# Simple TMDB Api Example

This project includes Webpack, Babel.

## Installation
In `src/js/config.js` please replace `YOUR_API_KEY` with your api key. 
You can get an api key from [tmdb](https://developers.themoviedb.org/).

After, just run:

```bash
npm install
npm run start
```